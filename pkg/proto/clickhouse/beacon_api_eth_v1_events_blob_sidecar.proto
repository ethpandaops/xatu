syntax = "proto3";

package clickhouse.v1;

import "common.proto";
import "google/protobuf/wrappers.proto";

option go_package = "github.com/ethpandaops/xatu/pkg/proto/clickhouse";
// Contains beacon API eventstream "blob_sidecar" data from each sentry client attached to a beacon node.

message BeaconApiEthV1EventsBlobSidecar {
  // Timestamp when the record was last updated
  uint32 updated_date_time = 11;
  // When the sentry received the event from a beacon node
  int64 event_date_time = 12;
  // Slot number in the beacon API event stream payload
  uint32 slot = 13;
  // The wall clock time when the slot started
  uint32 slot_start_date_time = 14;
  // The difference between the event_date_time and the slot_start_date_time
  uint32 propagation_slot_start_diff = 15;
  // The epoch number in the beacon API event stream payload
  uint32 epoch = 16;
  // The wall clock time when the epoch started
  uint32 epoch_start_date_time = 17;
  // The beacon block root hash in the beacon API event stream payload
  string block_root = 18;
  // The index of blob sidecar in the beacon API event stream payload
  uint64 blob_index = 19;
  // The KZG commitment in the beacon API event stream payload
  string kzg_commitment = 20;
  // The versioned hash in the beacon API event stream payload
  string versioned_hash = 21;
  // Name of the client that generated the event
  string meta_client_name = 22;
  // Version of the client that generated the event
  string meta_client_version = 23;
  // Implementation of the client that generated the event
  string meta_client_implementation = 24;
  // Operating system of the client that generated the event
  string meta_client_os = 25;
  // IP address of the client that generated the event
  google.protobuf.StringValue meta_client_ip = 26;
  // City of the client that generated the event
  string meta_client_geo_city = 27;
  // Country of the client that generated the event
  string meta_client_geo_country = 28;
  // Country code of the client that generated the event
  string meta_client_geo_country_code = 29;
  // Continent code of the client that generated the event
  string meta_client_geo_continent_code = 30;
  // Longitude of the client that generated the event
  google.protobuf.DoubleValue meta_client_geo_longitude = 31;
  // Latitude of the client that generated the event
  google.protobuf.DoubleValue meta_client_geo_latitude = 32;
  // Autonomous system number of the client that generated the event
  google.protobuf.UInt32Value meta_client_geo_autonomous_system_number = 33;
  // Autonomous system organization of the client that generated the event
  google.protobuf.StringValue meta_client_geo_autonomous_system_organization = 34;
  // Ethereum network name
  string meta_network_name = 35;
  // Ethereum consensus client version that generated the event
  string meta_consensus_version = 36;
  // Ethereum consensus client major version that generated the event
  string meta_consensus_version_major = 37;
  // Ethereum consensus client minor version that generated the event
  string meta_consensus_version_minor = 38;
  // Ethereum consensus client patch version that generated the event
  string meta_consensus_version_patch = 39;
  // Ethereum consensus client implementation that generated the event
  string meta_consensus_implementation = 40;
}

// Request for listing beacon_api_eth_v1_events_blob_sidecar records
message ListBeaconApiEthV1EventsBlobSidecarRequest {
  // Filter by meta_network_name - Ethereum network name (PRIMARY KEY - required)
  StringFilter meta_network_name = 1;

  // Filter by slot_start_date_time - The wall clock time when the slot started (ORDER BY column 2 - optional)
  UInt32Filter slot_start_date_time = 2;

  // Filter by meta_client_name - Name of the client that generated the event (ORDER BY column 3 - optional)
  StringFilter meta_client_name = 3;

  // Filter by block_root - The beacon block root hash in the beacon API event stream payload (ORDER BY column 4 - optional)
  StringFilter block_root = 4;

  // Filter by blob_index - The index of blob sidecar in the beacon API event stream payload (ORDER BY column 5 - optional)
  UInt64Filter blob_index = 5;

  // Filter by updated_date_time - Timestamp when the record was last updated (optional)
  UInt32Filter updated_date_time = 6;
  // Filter by event_date_time - When the sentry received the event from a beacon node (optional)
  Int64Filter event_date_time = 7;
  // Filter by slot - Slot number in the beacon API event stream payload (optional)
  UInt32Filter slot = 8;
  // Filter by propagation_slot_start_diff - The difference between the event_date_time and the slot_start_date_time (optional)
  UInt32Filter propagation_slot_start_diff = 9;
  // Filter by epoch - The epoch number in the beacon API event stream payload (optional)
  UInt32Filter epoch = 10;
  // Filter by epoch_start_date_time - The wall clock time when the epoch started (optional)
  UInt32Filter epoch_start_date_time = 11;
  // Filter by kzg_commitment - The KZG commitment in the beacon API event stream payload (optional)
  StringFilter kzg_commitment = 12;
  // Filter by versioned_hash - The versioned hash in the beacon API event stream payload (optional)
  StringFilter versioned_hash = 13;
  // Filter by meta_client_version - Version of the client that generated the event (optional)
  StringFilter meta_client_version = 14;
  // Filter by meta_client_implementation - Implementation of the client that generated the event (optional)
  StringFilter meta_client_implementation = 15;
  // Filter by meta_client_os - Operating system of the client that generated the event (optional)
  StringFilter meta_client_os = 16;
  // Filter by meta_client_ip - IP address of the client that generated the event (optional)
  NullableStringFilter meta_client_ip = 17;
  // Filter by meta_client_geo_city - City of the client that generated the event (optional)
  StringFilter meta_client_geo_city = 18;
  // Filter by meta_client_geo_country - Country of the client that generated the event (optional)
  StringFilter meta_client_geo_country = 19;
  // Filter by meta_client_geo_country_code - Country code of the client that generated the event (optional)
  StringFilter meta_client_geo_country_code = 20;
  // Filter by meta_client_geo_continent_code - Continent code of the client that generated the event (optional)
  StringFilter meta_client_geo_continent_code = 21;
  // Filter by meta_client_geo_longitude - Longitude of the client that generated the event (optional)
  google.protobuf.DoubleValue meta_client_geo_longitude = 22;
  // Filter by meta_client_geo_latitude - Latitude of the client that generated the event (optional)
  google.protobuf.DoubleValue meta_client_geo_latitude = 23;
  // Filter by meta_client_geo_autonomous_system_number - Autonomous system number of the client that generated the event (optional)
  NullableUInt32Filter meta_client_geo_autonomous_system_number = 24;
  // Filter by meta_client_geo_autonomous_system_organization - Autonomous system organization of the client that generated the event (optional)
  NullableStringFilter meta_client_geo_autonomous_system_organization = 25;
  // Filter by meta_consensus_version - Ethereum consensus client version that generated the event (optional)
  StringFilter meta_consensus_version = 26;
  // Filter by meta_consensus_version_major - Ethereum consensus client major version that generated the event (optional)
  StringFilter meta_consensus_version_major = 27;
  // Filter by meta_consensus_version_minor - Ethereum consensus client minor version that generated the event (optional)
  StringFilter meta_consensus_version_minor = 28;
  // Filter by meta_consensus_version_patch - Ethereum consensus client patch version that generated the event (optional)
  StringFilter meta_consensus_version_patch = 29;
  // Filter by meta_consensus_implementation - Ethereum consensus client implementation that generated the event (optional)
  StringFilter meta_consensus_implementation = 30;

  // The maximum number of beacon_api_eth_v1_events_blob_sidecar to return.
  // If unspecified, at most 100 items will be returned.
  // The maximum value is 10000; values above 10000 will be coerced to 10000.
  int32 page_size = 31;
  // A page token, received from a previous `ListBeaconApiEthV1EventsBlobSidecar` call.
  // Provide this to retrieve the subsequent page.
  string page_token = 32;
  // The order of results. Format: comma-separated list of fields.
  // Example: "foo,bar" or "foo desc,bar" for descending order on foo.
  // If unspecified, results will be returned in the default order.
  string order_by = 33;
}

// Response for listing beacon_api_eth_v1_events_blob_sidecar records
message ListBeaconApiEthV1EventsBlobSidecarResponse {
  // The list of beacon_api_eth_v1_events_blob_sidecar.
  repeated BeaconApiEthV1EventsBlobSidecar beacon_api_eth_v1_events_blob_sidecar = 1;
  // A token, which can be sent as `page_token` to retrieve the next page.
  // If this field is omitted, there are no subsequent pages.
  string next_page_token = 2;
}

// Request for getting a single beacon_api_eth_v1_events_blob_sidecar record by primary key
message GetBeaconApiEthV1EventsBlobSidecarRequest {
  // Ethereum network name
  string meta_network_name = 1; // Primary key (required)
}

// Response for getting a single beacon_api_eth_v1_events_blob_sidecar record
message GetBeaconApiEthV1EventsBlobSidecarResponse {
  BeaconApiEthV1EventsBlobSidecar item = 1;
}

// Query beacon_api_eth_v1_events_blob_sidecar data
service BeaconApiEthV1EventsBlobSidecarService {
  // List records | Retrieve paginated results with optional filtering
  rpc List(ListBeaconApiEthV1EventsBlobSidecarRequest) returns (ListBeaconApiEthV1EventsBlobSidecarResponse);
  // Get record | Retrieve a single record by primary key
  rpc Get(GetBeaconApiEthV1EventsBlobSidecarRequest) returns (GetBeaconApiEthV1EventsBlobSidecarResponse);
}
