syntax = "proto3";

package clickhouse.v1;

import "common.proto";
import "google/protobuf/wrappers.proto";

option go_package = "github.com/ethpandaops/xatu/pkg/proto/clickhouse";
// Contains beacon API eventstream "chain reorg" data from each sentry client attached to a beacon node.

message BeaconApiEthV1EventsChainReorg {
  // Timestamp when the record was last updated
  uint32 updated_date_time = 11;
  int64 event_date_time = 12;
  // The slot number of the chain reorg event in the beacon API event stream payload
  uint32 slot = 13;
  // The wall clock time when the reorg slot started
  uint32 slot_start_date_time = 14;
  // Difference in slots between when the reorg occurred and when the sentry received the event
  uint32 propagation_slot_start_diff = 15;
  // The depth of the chain reorg in the beacon API event stream payload
  uint32 depth = 16;
  // The old head block root hash in the beacon API event stream payload
  string old_head_block = 17;
  // The new head block root hash in the beacon API event stream payload
  string new_head_block = 18;
  // The old head state root hash in the beacon API event stream payload
  string old_head_state = 19;
  // The new head state root hash in the beacon API event stream payload
  string new_head_state = 20;
  // The epoch number in the beacon API event stream payload
  uint32 epoch = 21;
  // The wall clock time when the epoch started
  uint32 epoch_start_date_time = 22;
  // Whether the execution of the epoch was optimistic
  bool execution_optimistic = 23;
  // Name of the client that generated the event
  string meta_client_name = 24;
  // Version of the client that generated the event
  string meta_client_version = 25;
  // Implementation of the client that generated the event
  string meta_client_implementation = 26;
  // Operating system of the client that generated the event
  string meta_client_os = 27;
  // IP address of the client that generated the event
  google.protobuf.StringValue meta_client_ip = 28;
  // City of the client that generated the event
  string meta_client_geo_city = 29;
  // Country of the client that generated the event
  string meta_client_geo_country = 30;
  // Country code of the client that generated the event
  string meta_client_geo_country_code = 31;
  // Continent code of the client that generated the event
  string meta_client_geo_continent_code = 32;
  // Longitude of the client that generated the event
  google.protobuf.DoubleValue meta_client_geo_longitude = 33;
  // Latitude of the client that generated the event
  google.protobuf.DoubleValue meta_client_geo_latitude = 34;
  // Autonomous system number of the client that generated the event
  google.protobuf.UInt32Value meta_client_geo_autonomous_system_number = 35;
  // Autonomous system organization of the client that generated the event
  google.protobuf.StringValue meta_client_geo_autonomous_system_organization = 36;
  // Ethereum network name
  string meta_network_name = 37;
  // Ethereum consensus client version that generated the event
  string meta_consensus_version = 38;
  // Ethereum consensus client major version that generated the event
  string meta_consensus_version_major = 39;
  // Ethereum consensus client minor version that generated the event
  string meta_consensus_version_minor = 40;
  // Ethereum consensus client patch version that generated the event
  string meta_consensus_version_patch = 41;
  // Ethereum consensus client implementation that generated the event
  string meta_consensus_implementation = 42;
}

// Request for listing beacon_api_eth_v1_events_chain_reorg records
message ListBeaconApiEthV1EventsChainReorgRequest {
  // Filter by meta_network_name - Ethereum network name (PRIMARY KEY - required)
  StringFilter meta_network_name = 1;

  // Filter by slot_start_date_time - The wall clock time when the reorg slot started (ORDER BY column 2 - optional)
  UInt32Filter slot_start_date_time = 2;

  // Filter by meta_client_name - Name of the client that generated the event (ORDER BY column 3 - optional)
  StringFilter meta_client_name = 3;

  // Filter by old_head_block - The old head block root hash in the beacon API event stream payload (ORDER BY column 4 - optional)
  StringFilter old_head_block = 4;

  // Filter by new_head_block - The new head block root hash in the beacon API event stream payload (ORDER BY column 5 - optional)
  StringFilter new_head_block = 5;

  // Filter by updated_date_time - Timestamp when the record was last updated (optional)
  UInt32Filter updated_date_time = 6;
  // Filter by event_date_time (optional)
  Int64Filter event_date_time = 7;
  // Filter by slot - The slot number of the chain reorg event in the beacon API event stream payload (optional)
  UInt32Filter slot = 8;
  // Filter by propagation_slot_start_diff - Difference in slots between when the reorg occurred and when the sentry received the event (optional)
  UInt32Filter propagation_slot_start_diff = 9;
  // Filter by depth - The depth of the chain reorg in the beacon API event stream payload (optional)
  UInt32Filter depth = 10;
  // Filter by old_head_state - The old head state root hash in the beacon API event stream payload (optional)
  StringFilter old_head_state = 11;
  // Filter by new_head_state - The new head state root hash in the beacon API event stream payload (optional)
  StringFilter new_head_state = 12;
  // Filter by epoch - The epoch number in the beacon API event stream payload (optional)
  UInt32Filter epoch = 13;
  // Filter by epoch_start_date_time - The wall clock time when the epoch started (optional)
  UInt32Filter epoch_start_date_time = 14;
  // Filter by execution_optimistic - Whether the execution of the epoch was optimistic (optional)
  BoolFilter execution_optimistic = 15;
  // Filter by meta_client_version - Version of the client that generated the event (optional)
  StringFilter meta_client_version = 16;
  // Filter by meta_client_implementation - Implementation of the client that generated the event (optional)
  StringFilter meta_client_implementation = 17;
  // Filter by meta_client_os - Operating system of the client that generated the event (optional)
  StringFilter meta_client_os = 18;
  // Filter by meta_client_ip - IP address of the client that generated the event (optional)
  NullableStringFilter meta_client_ip = 19;
  // Filter by meta_client_geo_city - City of the client that generated the event (optional)
  StringFilter meta_client_geo_city = 20;
  // Filter by meta_client_geo_country - Country of the client that generated the event (optional)
  StringFilter meta_client_geo_country = 21;
  // Filter by meta_client_geo_country_code - Country code of the client that generated the event (optional)
  StringFilter meta_client_geo_country_code = 22;
  // Filter by meta_client_geo_continent_code - Continent code of the client that generated the event (optional)
  StringFilter meta_client_geo_continent_code = 23;
  // Filter by meta_client_geo_longitude - Longitude of the client that generated the event (optional)
  google.protobuf.DoubleValue meta_client_geo_longitude = 24;
  // Filter by meta_client_geo_latitude - Latitude of the client that generated the event (optional)
  google.protobuf.DoubleValue meta_client_geo_latitude = 25;
  // Filter by meta_client_geo_autonomous_system_number - Autonomous system number of the client that generated the event (optional)
  NullableUInt32Filter meta_client_geo_autonomous_system_number = 26;
  // Filter by meta_client_geo_autonomous_system_organization - Autonomous system organization of the client that generated the event (optional)
  NullableStringFilter meta_client_geo_autonomous_system_organization = 27;
  // Filter by meta_consensus_version - Ethereum consensus client version that generated the event (optional)
  StringFilter meta_consensus_version = 28;
  // Filter by meta_consensus_version_major - Ethereum consensus client major version that generated the event (optional)
  StringFilter meta_consensus_version_major = 29;
  // Filter by meta_consensus_version_minor - Ethereum consensus client minor version that generated the event (optional)
  StringFilter meta_consensus_version_minor = 30;
  // Filter by meta_consensus_version_patch - Ethereum consensus client patch version that generated the event (optional)
  StringFilter meta_consensus_version_patch = 31;
  // Filter by meta_consensus_implementation - Ethereum consensus client implementation that generated the event (optional)
  StringFilter meta_consensus_implementation = 32;

  // The maximum number of beacon_api_eth_v1_events_chain_reorg to return.
  // If unspecified, at most 100 items will be returned.
  // The maximum value is 10000; values above 10000 will be coerced to 10000.
  int32 page_size = 33;
  // A page token, received from a previous `ListBeaconApiEthV1EventsChainReorg` call.
  // Provide this to retrieve the subsequent page.
  string page_token = 34;
  // The order of results. Format: comma-separated list of fields.
  // Example: "foo,bar" or "foo desc,bar" for descending order on foo.
  // If unspecified, results will be returned in the default order.
  string order_by = 35;
}

// Response for listing beacon_api_eth_v1_events_chain_reorg records
message ListBeaconApiEthV1EventsChainReorgResponse {
  // The list of beacon_api_eth_v1_events_chain_reorg.
  repeated BeaconApiEthV1EventsChainReorg beacon_api_eth_v1_events_chain_reorg = 1;
  // A token, which can be sent as `page_token` to retrieve the next page.
  // If this field is omitted, there are no subsequent pages.
  string next_page_token = 2;
}

// Request for getting a single beacon_api_eth_v1_events_chain_reorg record by primary key
message GetBeaconApiEthV1EventsChainReorgRequest {
  // Ethereum network name
  string meta_network_name = 1; // Primary key (required)
}

// Response for getting a single beacon_api_eth_v1_events_chain_reorg record
message GetBeaconApiEthV1EventsChainReorgResponse {
  BeaconApiEthV1EventsChainReorg item = 1;
}

// Query beacon_api_eth_v1_events_chain_reorg data
service BeaconApiEthV1EventsChainReorgService {
  // List records | Retrieve paginated results with optional filtering
  rpc List(ListBeaconApiEthV1EventsChainReorgRequest) returns (ListBeaconApiEthV1EventsChainReorgResponse);
  // Get record | Retrieve a single record by primary key
  rpc Get(GetBeaconApiEthV1EventsChainReorgRequest) returns (GetBeaconApiEthV1EventsChainReorgResponse);
}
