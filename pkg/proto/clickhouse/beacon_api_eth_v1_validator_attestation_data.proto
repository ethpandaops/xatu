syntax = "proto3";

package clickhouse.v1;

import "common.proto";
import "google/protobuf/wrappers.proto";

option go_package = "github.com/ethpandaops/xatu/pkg/proto/clickhouse";
// Contains beacon API validator attestation data from each sentry client attached to a beacon node.

message BeaconApiEthV1ValidatorAttestationData {
  // Timestamp when the record was last updated
  uint32 updated_date_time = 11;
  int64 event_date_time = 12;
  // Slot number in the beacon API validator attestation data payload
  uint32 slot = 13;
  // The wall clock time when the slot started
  uint32 slot_start_date_time = 14;
  // The committee index in the beacon API validator attestation data payload
  string committee_index = 15;
  // The beacon block root hash in the beacon API validator attestation data payload
  string beacon_block_root = 16;
  // The epoch number in the beacon API validator attestation data payload
  uint32 epoch = 17;
  // The wall clock time when the epoch started
  uint32 epoch_start_date_time = 18;
  // The source epoch number in the beacon API validator attestation data payload
  uint32 source_epoch = 19;
  // The wall clock time when the source epoch started
  uint32 source_epoch_start_date_time = 20;
  // The source beacon block root hash in the beacon API validator attestation data payload
  string source_root = 21;
  // The target epoch number in the beacon API validator attestation data payload
  uint32 target_epoch = 22;
  // The wall clock time when the target epoch started
  uint32 target_epoch_start_date_time = 23;
  // The target beacon block root hash in the beacon API validator attestation data payload
  string target_root = 24;
  // When the request was sent to the beacon node
  uint32 request_date_time = 25;
  // The request duration in milliseconds
  uint32 request_duration = 26;
  // The difference between the request_date_time and the slot_start_date_time
  uint32 request_slot_start_diff = 27;
  // Name of the client that generated the event
  string meta_client_name = 28;
  // Version of the client that generated the event
  string meta_client_version = 29;
  // Implementation of the client that generated the event
  string meta_client_implementation = 30;
  // Operating system of the client that generated the event
  string meta_client_os = 31;
  // IP address of the client that generated the event
  google.protobuf.StringValue meta_client_ip = 32;
  // City of the client that generated the event
  string meta_client_geo_city = 33;
  // Country of the client that generated the event
  string meta_client_geo_country = 34;
  // Country code of the client that generated the event
  string meta_client_geo_country_code = 35;
  // Continent code of the client that generated the event
  string meta_client_geo_continent_code = 36;
  // Longitude of the client that generated the event
  google.protobuf.DoubleValue meta_client_geo_longitude = 37;
  // Latitude of the client that generated the event
  google.protobuf.DoubleValue meta_client_geo_latitude = 38;
  // Autonomous system number of the client that generated the event
  google.protobuf.UInt32Value meta_client_geo_autonomous_system_number = 39;
  // Autonomous system organization of the client that generated the event
  google.protobuf.StringValue meta_client_geo_autonomous_system_organization = 40;
  // Ethereum network name
  string meta_network_name = 41;
  // Ethereum consensus client version that generated the event
  string meta_consensus_version = 42;
  // Ethereum consensus client major version that generated the event
  string meta_consensus_version_major = 43;
  // Ethereum consensus client minor version that generated the event
  string meta_consensus_version_minor = 44;
  // Ethereum consensus client patch version that generated the event
  string meta_consensus_version_patch = 45;
  // Ethereum consensus client implementation that generated the event
  string meta_consensus_implementation = 46;
}

// Request for listing beacon_api_eth_v1_validator_attestation_data records
message ListBeaconApiEthV1ValidatorAttestationDataRequest {
  // Filter by meta_network_name - Ethereum network name (PRIMARY KEY - required)
  StringFilter meta_network_name = 1;

  // Filter by slot_start_date_time - The wall clock time when the slot started (ORDER BY column 2 - optional)
  UInt32Filter slot_start_date_time = 2;

  // Filter by meta_client_name - Name of the client that generated the event (ORDER BY column 3 - optional)
  StringFilter meta_client_name = 3;

  // Filter by committee_index - The committee index in the beacon API validator attestation data payload (ORDER BY column 4 - optional)
  StringFilter committee_index = 4;

  // Filter by beacon_block_root - The beacon block root hash in the beacon API validator attestation data payload (ORDER BY column 5 - optional)
  StringFilter beacon_block_root = 5;

  // Filter by source_root - The source beacon block root hash in the beacon API validator attestation data payload (ORDER BY column 6 - optional)
  StringFilter source_root = 6;

  // Filter by target_root - The target beacon block root hash in the beacon API validator attestation data payload (ORDER BY column 7 - optional)
  StringFilter target_root = 7;

  // Filter by updated_date_time - Timestamp when the record was last updated (optional)
  UInt32Filter updated_date_time = 8;
  // Filter by event_date_time (optional)
  Int64Filter event_date_time = 9;
  // Filter by slot - Slot number in the beacon API validator attestation data payload (optional)
  UInt32Filter slot = 10;
  // Filter by epoch - The epoch number in the beacon API validator attestation data payload (optional)
  UInt32Filter epoch = 11;
  // Filter by epoch_start_date_time - The wall clock time when the epoch started (optional)
  UInt32Filter epoch_start_date_time = 12;
  // Filter by source_epoch - The source epoch number in the beacon API validator attestation data payload (optional)
  UInt32Filter source_epoch = 13;
  // Filter by source_epoch_start_date_time - The wall clock time when the source epoch started (optional)
  UInt32Filter source_epoch_start_date_time = 14;
  // Filter by target_epoch - The target epoch number in the beacon API validator attestation data payload (optional)
  UInt32Filter target_epoch = 15;
  // Filter by target_epoch_start_date_time - The wall clock time when the target epoch started (optional)
  UInt32Filter target_epoch_start_date_time = 16;
  // Filter by request_date_time - When the request was sent to the beacon node (optional)
  UInt32Filter request_date_time = 17;
  // Filter by request_duration - The request duration in milliseconds (optional)
  UInt32Filter request_duration = 18;
  // Filter by request_slot_start_diff - The difference between the request_date_time and the slot_start_date_time (optional)
  UInt32Filter request_slot_start_diff = 19;
  // Filter by meta_client_version - Version of the client that generated the event (optional)
  StringFilter meta_client_version = 20;
  // Filter by meta_client_implementation - Implementation of the client that generated the event (optional)
  StringFilter meta_client_implementation = 21;
  // Filter by meta_client_os - Operating system of the client that generated the event (optional)
  StringFilter meta_client_os = 22;
  // Filter by meta_client_ip - IP address of the client that generated the event (optional)
  NullableStringFilter meta_client_ip = 23;
  // Filter by meta_client_geo_city - City of the client that generated the event (optional)
  StringFilter meta_client_geo_city = 24;
  // Filter by meta_client_geo_country - Country of the client that generated the event (optional)
  StringFilter meta_client_geo_country = 25;
  // Filter by meta_client_geo_country_code - Country code of the client that generated the event (optional)
  StringFilter meta_client_geo_country_code = 26;
  // Filter by meta_client_geo_continent_code - Continent code of the client that generated the event (optional)
  StringFilter meta_client_geo_continent_code = 27;
  // Filter by meta_client_geo_longitude - Longitude of the client that generated the event (optional)
  google.protobuf.DoubleValue meta_client_geo_longitude = 28;
  // Filter by meta_client_geo_latitude - Latitude of the client that generated the event (optional)
  google.protobuf.DoubleValue meta_client_geo_latitude = 29;
  // Filter by meta_client_geo_autonomous_system_number - Autonomous system number of the client that generated the event (optional)
  NullableUInt32Filter meta_client_geo_autonomous_system_number = 30;
  // Filter by meta_client_geo_autonomous_system_organization - Autonomous system organization of the client that generated the event (optional)
  NullableStringFilter meta_client_geo_autonomous_system_organization = 31;
  // Filter by meta_consensus_version - Ethereum consensus client version that generated the event (optional)
  StringFilter meta_consensus_version = 32;
  // Filter by meta_consensus_version_major - Ethereum consensus client major version that generated the event (optional)
  StringFilter meta_consensus_version_major = 33;
  // Filter by meta_consensus_version_minor - Ethereum consensus client minor version that generated the event (optional)
  StringFilter meta_consensus_version_minor = 34;
  // Filter by meta_consensus_version_patch - Ethereum consensus client patch version that generated the event (optional)
  StringFilter meta_consensus_version_patch = 35;
  // Filter by meta_consensus_implementation - Ethereum consensus client implementation that generated the event (optional)
  StringFilter meta_consensus_implementation = 36;

  // The maximum number of beacon_api_eth_v1_validator_attestation_data to return.
  // If unspecified, at most 100 items will be returned.
  // The maximum value is 10000; values above 10000 will be coerced to 10000.
  int32 page_size = 37;
  // A page token, received from a previous `ListBeaconApiEthV1ValidatorAttestationData` call.
  // Provide this to retrieve the subsequent page.
  string page_token = 38;
  // The order of results. Format: comma-separated list of fields.
  // Example: "foo,bar" or "foo desc,bar" for descending order on foo.
  // If unspecified, results will be returned in the default order.
  string order_by = 39;
}

// Response for listing beacon_api_eth_v1_validator_attestation_data records
message ListBeaconApiEthV1ValidatorAttestationDataResponse {
  // The list of beacon_api_eth_v1_validator_attestation_data.
  repeated BeaconApiEthV1ValidatorAttestationData beacon_api_eth_v1_validator_attestation_data = 1;
  // A token, which can be sent as `page_token` to retrieve the next page.
  // If this field is omitted, there are no subsequent pages.
  string next_page_token = 2;
}

// Request for getting a single beacon_api_eth_v1_validator_attestation_data record by primary key
message GetBeaconApiEthV1ValidatorAttestationDataRequest {
  // Ethereum network name
  string meta_network_name = 1; // Primary key (required)
}

// Response for getting a single beacon_api_eth_v1_validator_attestation_data record
message GetBeaconApiEthV1ValidatorAttestationDataResponse {
  BeaconApiEthV1ValidatorAttestationData item = 1;
}

// Query beacon_api_eth_v1_validator_attestation_data data
service BeaconApiEthV1ValidatorAttestationDataService {
  // List records | Retrieve paginated results with optional filtering
  rpc List(ListBeaconApiEthV1ValidatorAttestationDataRequest) returns (ListBeaconApiEthV1ValidatorAttestationDataResponse);
  // Get record | Retrieve a single record by primary key
  rpc Get(GetBeaconApiEthV1ValidatorAttestationDataRequest) returns (GetBeaconApiEthV1ValidatorAttestationDataResponse);
}
