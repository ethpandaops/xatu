syntax = "proto3";

package clickhouse.v1;

import "common.proto";
import "google/protobuf/wrappers.proto";

option go_package = "github.com/ethpandaops/xatu/pkg/proto/clickhouse";
// Contains beacon API /eth/v2/beacon/blocks/{block_id} data from each sentry client attached to a beacon node.

message BeaconApiEthV2BeaconBlock {
  // Timestamp when the record was last updated
  uint32 updated_date_time = 11;
  int64 event_date_time = 12;
  // The slot number from beacon block payload
  uint32 slot = 13;
  // The wall clock time when the reorg slot started
  uint32 slot_start_date_time = 14;
  // The epoch number from beacon block payload
  uint32 epoch = 15;
  // The wall clock time when the epoch started
  uint32 epoch_start_date_time = 16;
  // The root hash of the beacon block
  string block_root = 17;
  // The version of the beacon block
  string block_version = 18;
  // The total bytes of the beacon block payload
  google.protobuf.UInt32Value block_total_bytes = 19;
  // The total bytes of the beacon block payload when compressed using snappy
  google.protobuf.UInt32Value block_total_bytes_compressed = 20;
  // The root hash of the parent beacon block
  string parent_root = 21;
  // The root hash of the beacon state at this block
  string state_root = 22;
  // The index of the validator that proposed the beacon block
  uint32 proposer_index = 23;
  // The block hash of the associated execution block
  string eth1_data_block_hash = 24;
  // The root of the deposit tree in the associated execution block
  string eth1_data_deposit_root = 25;
  // The block hash of the execution payload
  google.protobuf.StringValue execution_payload_block_hash = 26;
  // The block number of the execution payload
  google.protobuf.UInt32Value execution_payload_block_number = 27;
  // The recipient of the fee for this execution payload
  google.protobuf.StringValue execution_payload_fee_recipient = 28;
  // Base fee per gas for execution payload
  google.protobuf.StringValue execution_payload_base_fee_per_gas = 29;
  // Gas used for blobs in execution payload
  google.protobuf.UInt64Value execution_payload_blob_gas_used = 30;
  // Excess gas used for blobs in execution payload
  google.protobuf.UInt64Value execution_payload_excess_blob_gas = 31;
  // Gas limit for execution payload
  google.protobuf.UInt64Value execution_payload_gas_limit = 32;
  // Gas used for execution payload
  google.protobuf.UInt64Value execution_payload_gas_used = 33;
  // The state root of the execution payload
  string execution_payload_state_root = 34;
  // The parent hash of the execution payload
  string execution_payload_parent_hash = 35;
  // The transaction count of the execution payload
  google.protobuf.UInt32Value execution_payload_transactions_count = 36;
  // The transaction total bytes of the execution payload
  google.protobuf.UInt32Value execution_payload_transactions_total_bytes = 37;
  // The transaction total bytes of the execution payload when compressed using snappy
  google.protobuf.UInt32Value execution_payload_transactions_total_bytes_compressed = 38;
  // Name of the client that generated the event
  string meta_client_name = 39;
  // Version of the client that generated the event
  string meta_client_version = 40;
  // Implementation of the client that generated the event
  string meta_client_implementation = 41;
  // Operating system of the client that generated the event
  string meta_client_os = 42;
  // IP address of the client that generated the event
  google.protobuf.StringValue meta_client_ip = 43;
  // City of the client that generated the event
  string meta_client_geo_city = 44;
  // Country of the client that generated the event
  string meta_client_geo_country = 45;
  // Country code of the client that generated the event
  string meta_client_geo_country_code = 46;
  // Continent code of the client that generated the event
  string meta_client_geo_continent_code = 47;
  // Longitude of the client that generated the event
  google.protobuf.DoubleValue meta_client_geo_longitude = 48;
  // Latitude of the client that generated the event
  google.protobuf.DoubleValue meta_client_geo_latitude = 49;
  // Autonomous system number of the client that generated the event
  google.protobuf.UInt32Value meta_client_geo_autonomous_system_number = 50;
  // Autonomous system organization of the client that generated the event
  google.protobuf.StringValue meta_client_geo_autonomous_system_organization = 51;
  // Ethereum network name
  string meta_network_name = 52;
  // Ethereum consensus client version that generated the event
  string meta_consensus_version = 53;
  // Ethereum consensus client major version that generated the event
  string meta_consensus_version_major = 54;
  // Ethereum consensus client minor version that generated the event
  string meta_consensus_version_minor = 55;
  // Ethereum consensus client patch version that generated the event
  string meta_consensus_version_patch = 56;
  // Ethereum consensus client implementation that generated the event
  string meta_consensus_implementation = 57;
}

// Request for listing beacon_api_eth_v2_beacon_block records
message ListBeaconApiEthV2BeaconBlockRequest {
  // Filter by meta_network_name - Ethereum network name (PRIMARY KEY - required)
  StringFilter meta_network_name = 1;

  // Filter by slot_start_date_time - The wall clock time when the reorg slot started (ORDER BY column 2 - optional)
  UInt32Filter slot_start_date_time = 2;

  // Filter by meta_client_name - Name of the client that generated the event (ORDER BY column 3 - optional)
  StringFilter meta_client_name = 3;

  // Filter by block_root - The root hash of the beacon block (ORDER BY column 4 - optional)
  StringFilter block_root = 4;

  // Filter by parent_root - The root hash of the parent beacon block (ORDER BY column 5 - optional)
  StringFilter parent_root = 5;

  // Filter by state_root - The root hash of the beacon state at this block (ORDER BY column 6 - optional)
  StringFilter state_root = 6;

  // Filter by updated_date_time - Timestamp when the record was last updated (optional)
  UInt32Filter updated_date_time = 7;
  // Filter by event_date_time (optional)
  Int64Filter event_date_time = 8;
  // Filter by slot - The slot number from beacon block payload (optional)
  UInt32Filter slot = 9;
  // Filter by epoch - The epoch number from beacon block payload (optional)
  UInt32Filter epoch = 10;
  // Filter by epoch_start_date_time - The wall clock time when the epoch started (optional)
  UInt32Filter epoch_start_date_time = 11;
  // Filter by block_version - The version of the beacon block (optional)
  StringFilter block_version = 12;
  // Filter by block_total_bytes - The total bytes of the beacon block payload (optional)
  NullableUInt32Filter block_total_bytes = 13;
  // Filter by block_total_bytes_compressed - The total bytes of the beacon block payload when compressed using snappy (optional)
  NullableUInt32Filter block_total_bytes_compressed = 14;
  // Filter by proposer_index - The index of the validator that proposed the beacon block (optional)
  UInt32Filter proposer_index = 15;
  // Filter by eth1_data_block_hash - The block hash of the associated execution block (optional)
  StringFilter eth1_data_block_hash = 16;
  // Filter by eth1_data_deposit_root - The root of the deposit tree in the associated execution block (optional)
  StringFilter eth1_data_deposit_root = 17;
  // Filter by execution_payload_block_hash - The block hash of the execution payload (optional)
  NullableStringFilter execution_payload_block_hash = 18;
  // Filter by execution_payload_block_number - The block number of the execution payload (optional)
  NullableUInt32Filter execution_payload_block_number = 19;
  // Filter by execution_payload_fee_recipient - The recipient of the fee for this execution payload (optional)
  NullableStringFilter execution_payload_fee_recipient = 20;
  // Filter by execution_payload_base_fee_per_gas - Base fee per gas for execution payload (optional)
  NullableStringFilter execution_payload_base_fee_per_gas = 21;
  // Filter by execution_payload_blob_gas_used - Gas used for blobs in execution payload (optional)
  NullableUInt64Filter execution_payload_blob_gas_used = 22;
  // Filter by execution_payload_excess_blob_gas - Excess gas used for blobs in execution payload (optional)
  NullableUInt64Filter execution_payload_excess_blob_gas = 23;
  // Filter by execution_payload_gas_limit - Gas limit for execution payload (optional)
  NullableUInt64Filter execution_payload_gas_limit = 24;
  // Filter by execution_payload_gas_used - Gas used for execution payload (optional)
  NullableUInt64Filter execution_payload_gas_used = 25;
  // Filter by execution_payload_state_root - The state root of the execution payload (optional)
  StringFilter execution_payload_state_root = 26;
  // Filter by execution_payload_parent_hash - The parent hash of the execution payload (optional)
  StringFilter execution_payload_parent_hash = 27;
  // Filter by execution_payload_transactions_count - The transaction count of the execution payload (optional)
  NullableUInt32Filter execution_payload_transactions_count = 28;
  // Filter by execution_payload_transactions_total_bytes - The transaction total bytes of the execution payload (optional)
  NullableUInt32Filter execution_payload_transactions_total_bytes = 29;
  // Filter by execution_payload_transactions_total_bytes_compressed - The transaction total bytes of the execution payload when compressed using snappy (optional)
  NullableUInt32Filter execution_payload_transactions_total_bytes_compressed = 30;
  // Filter by meta_client_version - Version of the client that generated the event (optional)
  StringFilter meta_client_version = 31;
  // Filter by meta_client_implementation - Implementation of the client that generated the event (optional)
  StringFilter meta_client_implementation = 32;
  // Filter by meta_client_os - Operating system of the client that generated the event (optional)
  StringFilter meta_client_os = 33;
  // Filter by meta_client_ip - IP address of the client that generated the event (optional)
  NullableStringFilter meta_client_ip = 34;
  // Filter by meta_client_geo_city - City of the client that generated the event (optional)
  StringFilter meta_client_geo_city = 35;
  // Filter by meta_client_geo_country - Country of the client that generated the event (optional)
  StringFilter meta_client_geo_country = 36;
  // Filter by meta_client_geo_country_code - Country code of the client that generated the event (optional)
  StringFilter meta_client_geo_country_code = 37;
  // Filter by meta_client_geo_continent_code - Continent code of the client that generated the event (optional)
  StringFilter meta_client_geo_continent_code = 38;
  // Filter by meta_client_geo_longitude - Longitude of the client that generated the event (optional)
  google.protobuf.DoubleValue meta_client_geo_longitude = 39;
  // Filter by meta_client_geo_latitude - Latitude of the client that generated the event (optional)
  google.protobuf.DoubleValue meta_client_geo_latitude = 40;
  // Filter by meta_client_geo_autonomous_system_number - Autonomous system number of the client that generated the event (optional)
  NullableUInt32Filter meta_client_geo_autonomous_system_number = 41;
  // Filter by meta_client_geo_autonomous_system_organization - Autonomous system organization of the client that generated the event (optional)
  NullableStringFilter meta_client_geo_autonomous_system_organization = 42;
  // Filter by meta_consensus_version - Ethereum consensus client version that generated the event (optional)
  StringFilter meta_consensus_version = 43;
  // Filter by meta_consensus_version_major - Ethereum consensus client major version that generated the event (optional)
  StringFilter meta_consensus_version_major = 44;
  // Filter by meta_consensus_version_minor - Ethereum consensus client minor version that generated the event (optional)
  StringFilter meta_consensus_version_minor = 45;
  // Filter by meta_consensus_version_patch - Ethereum consensus client patch version that generated the event (optional)
  StringFilter meta_consensus_version_patch = 46;
  // Filter by meta_consensus_implementation - Ethereum consensus client implementation that generated the event (optional)
  StringFilter meta_consensus_implementation = 47;

  // The maximum number of beacon_api_eth_v2_beacon_block to return.
  // If unspecified, at most 100 items will be returned.
  // The maximum value is 10000; values above 10000 will be coerced to 10000.
  int32 page_size = 48;
  // A page token, received from a previous `ListBeaconApiEthV2BeaconBlock` call.
  // Provide this to retrieve the subsequent page.
  string page_token = 49;
  // The order of results. Format: comma-separated list of fields.
  // Example: "foo,bar" or "foo desc,bar" for descending order on foo.
  // If unspecified, results will be returned in the default order.
  string order_by = 50;
}

// Response for listing beacon_api_eth_v2_beacon_block records
message ListBeaconApiEthV2BeaconBlockResponse {
  // The list of beacon_api_eth_v2_beacon_block.
  repeated BeaconApiEthV2BeaconBlock beacon_api_eth_v2_beacon_block = 1;
  // A token, which can be sent as `page_token` to retrieve the next page.
  // If this field is omitted, there are no subsequent pages.
  string next_page_token = 2;
}

// Request for getting a single beacon_api_eth_v2_beacon_block record by primary key
message GetBeaconApiEthV2BeaconBlockRequest {
  // Ethereum network name
  string meta_network_name = 1; // Primary key (required)
}

// Response for getting a single beacon_api_eth_v2_beacon_block record
message GetBeaconApiEthV2BeaconBlockResponse {
  BeaconApiEthV2BeaconBlock item = 1;
}

// Query beacon_api_eth_v2_beacon_block data
service BeaconApiEthV2BeaconBlockService {
  // List records | Retrieve paginated results with optional filtering
  rpc List(ListBeaconApiEthV2BeaconBlockRequest) returns (ListBeaconApiEthV2BeaconBlockResponse);
  // Get record | Retrieve a single record by primary key
  rpc Get(GetBeaconApiEthV2BeaconBlockRequest) returns (GetBeaconApiEthV2BeaconBlockResponse);
}
