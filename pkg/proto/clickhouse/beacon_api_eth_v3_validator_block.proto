syntax = "proto3";

package clickhouse.v1;

import "common.proto";
import "google/protobuf/wrappers.proto";

option go_package = "github.com/ethpandaops/xatu/pkg/proto/clickhouse";
// Contains beacon API /eth/v3/validator/blocks/{slot} data from each sentry client attached to a beacon node.

message BeaconApiEthV3ValidatorBlock {
  // Timestamp when the record was last updated
  uint32 updated_date_time = 11;
  // When the sentry received the event from a beacon node
  int64 event_date_time = 12;
  // Slot number within the payload
  uint32 slot = 13;
  uint32 slot_start_date_time = 14;
  // The epoch number in the beacon API event stream payload
  uint32 epoch = 15;
  // The wall clock time when the epoch started
  uint32 epoch_start_date_time = 16;
  // The version of the beacon block
  string block_version = 17;
  // The total bytes of the beacon block payload
  google.protobuf.UInt32Value block_total_bytes = 18;
  // The total bytes of the beacon block payload when compressed using snappy
  google.protobuf.UInt32Value block_total_bytes_compressed = 19;
  // Consensus rewards paid to the proposer for this block, in Wei. Use to determine relative value of consensus blocks.
  google.protobuf.UInt64Value consensus_payload_value = 20;
  // Execution payload value in Wei. Use to determine relative value of execution payload.
  google.protobuf.UInt64Value execution_payload_value = 21;
  // The block number of the execution payload
  uint32 execution_payload_block_number = 22;
  // Base fee per gas for execution payload
  google.protobuf.StringValue execution_payload_base_fee_per_gas = 23;
  // Gas used for blobs in execution payload
  google.protobuf.UInt64Value execution_payload_blob_gas_used = 24;
  // Excess gas used for blobs in execution payload
  google.protobuf.UInt64Value execution_payload_excess_blob_gas = 25;
  // Gas limit for execution payload
  google.protobuf.UInt64Value execution_payload_gas_limit = 26;
  // Gas used for execution payload
  google.protobuf.UInt64Value execution_payload_gas_used = 27;
  // The transaction count of the execution payload
  google.protobuf.UInt32Value execution_payload_transactions_count = 28;
  // The transaction total bytes of the execution payload
  google.protobuf.UInt32Value execution_payload_transactions_total_bytes = 29;
  // The transaction total bytes of the execution payload when compressed using snappy
  google.protobuf.UInt32Value execution_payload_transactions_total_bytes_compressed = 30;
  // Name of the client that generated the event
  string meta_client_name = 31;
  // Version of the client that generated the event
  string meta_client_version = 32;
  // Implementation of the client that generated the event
  string meta_client_implementation = 33;
  // Operating system of the client that generated the event
  string meta_client_os = 34;
  // IP address of the client that generated the event
  google.protobuf.StringValue meta_client_ip = 35;
  // City of the client that generated the event
  string meta_client_geo_city = 36;
  // Country of the client that generated the event
  string meta_client_geo_country = 37;
  // Country code of the client that generated the event
  string meta_client_geo_country_code = 38;
  // Continent code of the client that generated the event
  string meta_client_geo_continent_code = 39;
  // Longitude of the client that generated the event
  google.protobuf.DoubleValue meta_client_geo_longitude = 40;
  // Latitude of the client that generated the event
  google.protobuf.DoubleValue meta_client_geo_latitude = 41;
  // Autonomous system number of the client that generated the event
  google.protobuf.UInt32Value meta_client_geo_autonomous_system_number = 42;
  // Autonomous system organization of the client that generated the event
  google.protobuf.StringValue meta_client_geo_autonomous_system_organization = 43;
  // Ethereum network name
  string meta_network_name = 44;
  // Ethereum consensus client version that generated the event
  string meta_consensus_version = 45;
  // Ethereum consensus client major version that generated the event
  string meta_consensus_version_major = 46;
  // Ethereum consensus client minor version that generated the event
  string meta_consensus_version_minor = 47;
  // Ethereum consensus client patch version that generated the event
  string meta_consensus_version_patch = 48;
  // Ethereum consensus client implementation that generated the event
  string meta_consensus_implementation = 49;
}

// Request for listing beacon_api_eth_v3_validator_block records
message ListBeaconApiEthV3ValidatorBlockRequest {
  // Filter by meta_network_name - Ethereum network name (PRIMARY KEY - required)
  StringFilter meta_network_name = 1;

  // Filter by slot_start_date_time (ORDER BY column 2 - optional)
  UInt32Filter slot_start_date_time = 2;

  // Filter by meta_client_name - Name of the client that generated the event (ORDER BY column 3 - optional)
  StringFilter meta_client_name = 3;

  // Filter by event_date_time - When the sentry received the event from a beacon node (ORDER BY column 4 - optional)
  Int64Filter event_date_time = 4;

  // Filter by updated_date_time - Timestamp when the record was last updated (optional)
  UInt32Filter updated_date_time = 5;
  // Filter by slot - Slot number within the payload (optional)
  UInt32Filter slot = 6;
  // Filter by epoch - The epoch number in the beacon API event stream payload (optional)
  UInt32Filter epoch = 7;
  // Filter by epoch_start_date_time - The wall clock time when the epoch started (optional)
  UInt32Filter epoch_start_date_time = 8;
  // Filter by block_version - The version of the beacon block (optional)
  StringFilter block_version = 9;
  // Filter by block_total_bytes - The total bytes of the beacon block payload (optional)
  NullableUInt32Filter block_total_bytes = 10;
  // Filter by block_total_bytes_compressed - The total bytes of the beacon block payload when compressed using snappy (optional)
  NullableUInt32Filter block_total_bytes_compressed = 11;
  // Filter by consensus_payload_value - Consensus rewards paid to the proposer for this block, in Wei. Use to determine relative value of consensus blocks. (optional)
  NullableUInt64Filter consensus_payload_value = 12;
  // Filter by execution_payload_value - Execution payload value in Wei. Use to determine relative value of execution payload. (optional)
  NullableUInt64Filter execution_payload_value = 13;
  // Filter by execution_payload_block_number - The block number of the execution payload (optional)
  UInt32Filter execution_payload_block_number = 14;
  // Filter by execution_payload_base_fee_per_gas - Base fee per gas for execution payload (optional)
  NullableStringFilter execution_payload_base_fee_per_gas = 15;
  // Filter by execution_payload_blob_gas_used - Gas used for blobs in execution payload (optional)
  NullableUInt64Filter execution_payload_blob_gas_used = 16;
  // Filter by execution_payload_excess_blob_gas - Excess gas used for blobs in execution payload (optional)
  NullableUInt64Filter execution_payload_excess_blob_gas = 17;
  // Filter by execution_payload_gas_limit - Gas limit for execution payload (optional)
  NullableUInt64Filter execution_payload_gas_limit = 18;
  // Filter by execution_payload_gas_used - Gas used for execution payload (optional)
  NullableUInt64Filter execution_payload_gas_used = 19;
  // Filter by execution_payload_transactions_count - The transaction count of the execution payload (optional)
  NullableUInt32Filter execution_payload_transactions_count = 20;
  // Filter by execution_payload_transactions_total_bytes - The transaction total bytes of the execution payload (optional)
  NullableUInt32Filter execution_payload_transactions_total_bytes = 21;
  // Filter by execution_payload_transactions_total_bytes_compressed - The transaction total bytes of the execution payload when compressed using snappy (optional)
  NullableUInt32Filter execution_payload_transactions_total_bytes_compressed = 22;
  // Filter by meta_client_version - Version of the client that generated the event (optional)
  StringFilter meta_client_version = 23;
  // Filter by meta_client_implementation - Implementation of the client that generated the event (optional)
  StringFilter meta_client_implementation = 24;
  // Filter by meta_client_os - Operating system of the client that generated the event (optional)
  StringFilter meta_client_os = 25;
  // Filter by meta_client_ip - IP address of the client that generated the event (optional)
  NullableStringFilter meta_client_ip = 26;
  // Filter by meta_client_geo_city - City of the client that generated the event (optional)
  StringFilter meta_client_geo_city = 27;
  // Filter by meta_client_geo_country - Country of the client that generated the event (optional)
  StringFilter meta_client_geo_country = 28;
  // Filter by meta_client_geo_country_code - Country code of the client that generated the event (optional)
  StringFilter meta_client_geo_country_code = 29;
  // Filter by meta_client_geo_continent_code - Continent code of the client that generated the event (optional)
  StringFilter meta_client_geo_continent_code = 30;
  // Filter by meta_client_geo_longitude - Longitude of the client that generated the event (optional)
  google.protobuf.DoubleValue meta_client_geo_longitude = 31;
  // Filter by meta_client_geo_latitude - Latitude of the client that generated the event (optional)
  google.protobuf.DoubleValue meta_client_geo_latitude = 32;
  // Filter by meta_client_geo_autonomous_system_number - Autonomous system number of the client that generated the event (optional)
  NullableUInt32Filter meta_client_geo_autonomous_system_number = 33;
  // Filter by meta_client_geo_autonomous_system_organization - Autonomous system organization of the client that generated the event (optional)
  NullableStringFilter meta_client_geo_autonomous_system_organization = 34;
  // Filter by meta_consensus_version - Ethereum consensus client version that generated the event (optional)
  StringFilter meta_consensus_version = 35;
  // Filter by meta_consensus_version_major - Ethereum consensus client major version that generated the event (optional)
  StringFilter meta_consensus_version_major = 36;
  // Filter by meta_consensus_version_minor - Ethereum consensus client minor version that generated the event (optional)
  StringFilter meta_consensus_version_minor = 37;
  // Filter by meta_consensus_version_patch - Ethereum consensus client patch version that generated the event (optional)
  StringFilter meta_consensus_version_patch = 38;
  // Filter by meta_consensus_implementation - Ethereum consensus client implementation that generated the event (optional)
  StringFilter meta_consensus_implementation = 39;

  // The maximum number of beacon_api_eth_v3_validator_block to return.
  // If unspecified, at most 100 items will be returned.
  // The maximum value is 10000; values above 10000 will be coerced to 10000.
  int32 page_size = 40;
  // A page token, received from a previous `ListBeaconApiEthV3ValidatorBlock` call.
  // Provide this to retrieve the subsequent page.
  string page_token = 41;
  // The order of results. Format: comma-separated list of fields.
  // Example: "foo,bar" or "foo desc,bar" for descending order on foo.
  // If unspecified, results will be returned in the default order.
  string order_by = 42;
}

// Response for listing beacon_api_eth_v3_validator_block records
message ListBeaconApiEthV3ValidatorBlockResponse {
  // The list of beacon_api_eth_v3_validator_block.
  repeated BeaconApiEthV3ValidatorBlock beacon_api_eth_v3_validator_block = 1;
  // A token, which can be sent as `page_token` to retrieve the next page.
  // If this field is omitted, there are no subsequent pages.
  string next_page_token = 2;
}

// Request for getting a single beacon_api_eth_v3_validator_block record by primary key
message GetBeaconApiEthV3ValidatorBlockRequest {
  // Ethereum network name
  string meta_network_name = 1; // Primary key (required)
}

// Response for getting a single beacon_api_eth_v3_validator_block record
message GetBeaconApiEthV3ValidatorBlockResponse {
  BeaconApiEthV3ValidatorBlock item = 1;
}

// Query beacon_api_eth_v3_validator_block data
service BeaconApiEthV3ValidatorBlockService {
  // List records | Retrieve paginated results with optional filtering
  rpc List(ListBeaconApiEthV3ValidatorBlockRequest) returns (ListBeaconApiEthV3ValidatorBlockResponse);
  // Get record | Retrieve a single record by primary key
  rpc Get(GetBeaconApiEthV3ValidatorBlockRequest) returns (GetBeaconApiEthV3ValidatorBlockResponse);
}
