syntax = "proto3";

package clickhouse.v1;

import "common.proto";
import "google/protobuf/wrappers.proto";

option go_package = "github.com/ethpandaops/xatu/pkg/proto/clickhouse";
// Contains timing and instrumentation data for engine_newPayload calls between the consensus and execution layer.

message ConsensusEngineApiNewPayload {
  // Timestamp when the record was last updated
  uint32 updated_date_time = 11;
  // When the sentry received the event
  int64 event_date_time = 12;
  // Timestamp when the engine_newPayload call was initiated
  int64 requested_date_time = 13;
  // How long the engine_newPayload call took in milliseconds
  uint64 duration_ms = 14;
  // Slot number of the beacon block containing the payload
  uint32 slot = 15;
  // The wall clock time when the slot started
  uint32 slot_start_date_time = 16;
  // Epoch number derived from the slot
  uint32 epoch = 17;
  // The wall clock time when the epoch started
  uint32 epoch_start_date_time = 18;
  // Root of the beacon block (hex encoded with 0x prefix)
  string block_root = 19;
  // Root of the parent beacon block (hex encoded with 0x prefix)
  string parent_block_root = 20;
  // Validator index of the block proposer
  uint32 proposer_index = 21;
  // Execution block number
  uint64 block_number = 22;
  // Execution block hash (hex encoded with 0x prefix)
  string block_hash = 23;
  // Parent execution block hash (hex encoded with 0x prefix)
  string parent_hash = 24;
  // Total gas used by all transactions in the block
  uint64 gas_used = 25;
  // Gas limit of the block
  uint64 gas_limit = 26;
  // Number of transactions in the block
  uint32 tx_count = 27;
  // Number of blobs in the block
  uint32 blob_count = 28;
  // Payload status returned by EL (VALID, INVALID, SYNCING, ACCEPTED, INVALID_BLOCK_HASH)
  string status = 29;
  // Latest valid hash when status is INVALID (hex encoded with 0x prefix)
  google.protobuf.StringValue latest_valid_hash = 30;
  // Error message when validation fails
  google.protobuf.StringValue validation_error = 31;
  // Version of the engine_newPayload method (e.g., V3, V4)
  string method_version = 32;
  string meta_execution_version = 33;
  // Execution client implementation name (e.g., Geth, Nethermind, Besu, Reth, Erigon)
  string meta_execution_implementation = 34;
  // Execution client major version number
  string meta_execution_version_major = 35;
  // Execution client minor version number
  string meta_execution_version_minor = 36;
  // Execution client patch version number
  string meta_execution_version_patch = 37;
  // Name of the client that generated the event
  string meta_client_name = 38;
  // Version of the client that generated the event
  string meta_client_version = 39;
  // Implementation of the client that generated the event
  string meta_client_implementation = 40;
  // Operating system of the client that generated the event
  string meta_client_os = 41;
  // IP address of the client that generated the event
  google.protobuf.StringValue meta_client_ip = 42;
  // City of the client that generated the event
  string meta_client_geo_city = 43;
  // Country of the client that generated the event
  string meta_client_geo_country = 44;
  // Country code of the client that generated the event
  string meta_client_geo_country_code = 45;
  // Continent code of the client that generated the event
  string meta_client_geo_continent_code = 46;
  // Longitude of the client that generated the event
  google.protobuf.DoubleValue meta_client_geo_longitude = 47;
  // Latitude of the client that generated the event
  google.protobuf.DoubleValue meta_client_geo_latitude = 48;
  // Autonomous system number of the client that generated the event
  google.protobuf.UInt32Value meta_client_geo_autonomous_system_number = 49;
  // Autonomous system organization of the client that generated the event
  google.protobuf.StringValue meta_client_geo_autonomous_system_organization = 50;
  // Ethereum network name
  string meta_network_name = 51;
}

// Request for listing consensus_engine_api_new_payload records
message ListConsensusEngineApiNewPayloadRequest {
  // Filter by meta_network_name - Ethereum network name (PRIMARY KEY - required)
  StringFilter meta_network_name = 1;

  // Filter by slot_start_date_time - The wall clock time when the slot started (ORDER BY column 2 - optional)
  UInt32Filter slot_start_date_time = 2;

  // Filter by meta_client_name - Name of the client that generated the event (ORDER BY column 3 - optional)
  StringFilter meta_client_name = 3;

  // Filter by block_hash - Execution block hash (hex encoded with 0x prefix) (ORDER BY column 4 - optional)
  StringFilter block_hash = 4;

  // Filter by event_date_time - When the sentry received the event (ORDER BY column 5 - optional)
  Int64Filter event_date_time = 5;

  // Filter by updated_date_time - Timestamp when the record was last updated (optional)
  UInt32Filter updated_date_time = 6;
  // Filter by requested_date_time - Timestamp when the engine_newPayload call was initiated (optional)
  Int64Filter requested_date_time = 7;
  // Filter by duration_ms - How long the engine_newPayload call took in milliseconds (optional)
  UInt64Filter duration_ms = 8;
  // Filter by slot - Slot number of the beacon block containing the payload (optional)
  UInt32Filter slot = 9;
  // Filter by epoch - Epoch number derived from the slot (optional)
  UInt32Filter epoch = 10;
  // Filter by epoch_start_date_time - The wall clock time when the epoch started (optional)
  UInt32Filter epoch_start_date_time = 11;
  // Filter by block_root - Root of the beacon block (hex encoded with 0x prefix) (optional)
  StringFilter block_root = 12;
  // Filter by parent_block_root - Root of the parent beacon block (hex encoded with 0x prefix) (optional)
  StringFilter parent_block_root = 13;
  // Filter by proposer_index - Validator index of the block proposer (optional)
  UInt32Filter proposer_index = 14;
  // Filter by block_number - Execution block number (optional)
  UInt64Filter block_number = 15;
  // Filter by parent_hash - Parent execution block hash (hex encoded with 0x prefix) (optional)
  StringFilter parent_hash = 16;
  // Filter by gas_used - Total gas used by all transactions in the block (optional)
  UInt64Filter gas_used = 17;
  // Filter by gas_limit - Gas limit of the block (optional)
  UInt64Filter gas_limit = 18;
  // Filter by tx_count - Number of transactions in the block (optional)
  UInt32Filter tx_count = 19;
  // Filter by blob_count - Number of blobs in the block (optional)
  UInt32Filter blob_count = 20;
  // Filter by status - Payload status returned by EL (VALID, INVALID, SYNCING, ACCEPTED, INVALID_BLOCK_HASH) (optional)
  StringFilter status = 21;
  // Filter by latest_valid_hash - Latest valid hash when status is INVALID (hex encoded with 0x prefix) (optional)
  NullableStringFilter latest_valid_hash = 22;
  // Filter by validation_error - Error message when validation fails (optional)
  NullableStringFilter validation_error = 23;
  // Filter by method_version - Version of the engine_newPayload method (e.g., V3, V4) (optional)
  StringFilter method_version = 24;
  // Filter by meta_execution_version (optional)
  StringFilter meta_execution_version = 25;
  // Filter by meta_execution_implementation - Execution client implementation name (e.g., Geth, Nethermind, Besu, Reth, Erigon) (optional)
  StringFilter meta_execution_implementation = 26;
  // Filter by meta_execution_version_major - Execution client major version number (optional)
  StringFilter meta_execution_version_major = 27;
  // Filter by meta_execution_version_minor - Execution client minor version number (optional)
  StringFilter meta_execution_version_minor = 28;
  // Filter by meta_execution_version_patch - Execution client patch version number (optional)
  StringFilter meta_execution_version_patch = 29;
  // Filter by meta_client_version - Version of the client that generated the event (optional)
  StringFilter meta_client_version = 30;
  // Filter by meta_client_implementation - Implementation of the client that generated the event (optional)
  StringFilter meta_client_implementation = 31;
  // Filter by meta_client_os - Operating system of the client that generated the event (optional)
  StringFilter meta_client_os = 32;
  // Filter by meta_client_ip - IP address of the client that generated the event (optional)
  NullableStringFilter meta_client_ip = 33;
  // Filter by meta_client_geo_city - City of the client that generated the event (optional)
  StringFilter meta_client_geo_city = 34;
  // Filter by meta_client_geo_country - Country of the client that generated the event (optional)
  StringFilter meta_client_geo_country = 35;
  // Filter by meta_client_geo_country_code - Country code of the client that generated the event (optional)
  StringFilter meta_client_geo_country_code = 36;
  // Filter by meta_client_geo_continent_code - Continent code of the client that generated the event (optional)
  StringFilter meta_client_geo_continent_code = 37;
  // Filter by meta_client_geo_longitude - Longitude of the client that generated the event (optional)
  google.protobuf.DoubleValue meta_client_geo_longitude = 38;
  // Filter by meta_client_geo_latitude - Latitude of the client that generated the event (optional)
  google.protobuf.DoubleValue meta_client_geo_latitude = 39;
  // Filter by meta_client_geo_autonomous_system_number - Autonomous system number of the client that generated the event (optional)
  NullableUInt32Filter meta_client_geo_autonomous_system_number = 40;
  // Filter by meta_client_geo_autonomous_system_organization - Autonomous system organization of the client that generated the event (optional)
  NullableStringFilter meta_client_geo_autonomous_system_organization = 41;

  // The maximum number of consensus_engine_api_new_payload to return.
  // If unspecified, at most 100 items will be returned.
  // The maximum value is 10000; values above 10000 will be coerced to 10000.
  int32 page_size = 42;
  // A page token, received from a previous `ListConsensusEngineApiNewPayload` call.
  // Provide this to retrieve the subsequent page.
  string page_token = 43;
  // The order of results. Format: comma-separated list of fields.
  // Example: "foo,bar" or "foo desc,bar" for descending order on foo.
  // If unspecified, results will be returned in the default order.
  string order_by = 44;
}

// Response for listing consensus_engine_api_new_payload records
message ListConsensusEngineApiNewPayloadResponse {
  // The list of consensus_engine_api_new_payload.
  repeated ConsensusEngineApiNewPayload consensus_engine_api_new_payload = 1;
  // A token, which can be sent as `page_token` to retrieve the next page.
  // If this field is omitted, there are no subsequent pages.
  string next_page_token = 2;
}

// Request for getting a single consensus_engine_api_new_payload record by primary key
message GetConsensusEngineApiNewPayloadRequest {
  // Ethereum network name
  string meta_network_name = 1; // Primary key (required)
}

// Response for getting a single consensus_engine_api_new_payload record
message GetConsensusEngineApiNewPayloadResponse {
  ConsensusEngineApiNewPayload item = 1;
}

// Query consensus_engine_api_new_payload data
service ConsensusEngineApiNewPayloadService {
  // List records | Retrieve paginated results with optional filtering
  rpc List(ListConsensusEngineApiNewPayloadRequest) returns (ListConsensusEngineApiNewPayloadResponse);
  // Get record | Retrieve a single record by primary key
  rpc Get(GetConsensusEngineApiNewPayloadRequest) returns (GetConsensusEngineApiNewPayloadResponse);
}
