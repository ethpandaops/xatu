syntax = "proto3";

package clickhouse.v1;

import "common.proto";
import "google/protobuf/wrappers.proto";

option go_package = "github.com/ethpandaops/xatu/pkg/proto/clickhouse";
// Table for libp2p gossipsub aggregate and proof data.

message Libp2pGossipsubAggregateAndProof {
  // Timestamp when the record was last updated
  uint32 updated_date_time = 11;
  // Timestamp of the event with millisecond precision
  int64 event_date_time = 12;
  // Slot number associated with the event
  uint32 slot = 13;
  // Start date and time of the slot
  uint32 slot_start_date_time = 14;
  // Epoch number associated with the event
  uint32 epoch = 15;
  // Start date and time of the epoch
  uint32 epoch_start_date_time = 16;
  // Slot number of the wall clock when the event was received
  uint32 wallclock_slot = 17;
  // Start date and time of the wall clock slot when the event was received
  uint32 wallclock_slot_start_date_time = 18;
  // Epoch number of the wall clock when the event was received
  uint32 wallclock_epoch = 19;
  // Start date and time of the wall clock epoch when the event was received
  uint32 wallclock_epoch_start_date_time = 20;
  // Difference in slot start time for propagation
  uint32 propagation_slot_start_diff = 21;
  // Unique key associated with the identifier of the peer
  int64 peer_id_unique_key = 22;
  // Identifier of the message
  string message_id = 23;
  // Size of the message in bytes
  uint32 message_size = 24;
  // Layer of the topic in the gossipsub protocol
  string topic_layer = 25;
  // Fork digest value of the topic
  string topic_fork_digest_value = 26;
  // Name of the topic
  string topic_name = 27;
  // Encoding used for the topic
  string topic_encoding = 28;
  // Index of the validator who created this aggregate
  uint32 aggregator_index = 29;
  // Committee index from the attestation
  string committee_index = 30;
  // Bitfield of aggregated attestation
  string aggregation_bits = 31;
  // Root of the beacon block being attested to
  string beacon_block_root = 32;
  // Source epoch from the attestation
  uint32 source_epoch = 33;
  // Source root from the attestation
  string source_root = 34;
  // Target epoch from the attestation
  uint32 target_epoch = 35;
  // Target root from the attestation
  string target_root = 36;
  // Name of the client that generated the event
  string meta_client_name = 37;
  // Version of the client that generated the event
  string meta_client_version = 38;
  // Implementation of the client that generated the event
  string meta_client_implementation = 39;
  // Operating system of the client that generated the event
  string meta_client_os = 40;
  // IP address of the client that generated the event
  google.protobuf.StringValue meta_client_ip = 41;
  // City of the client that generated the event
  string meta_client_geo_city = 42;
  // Country of the client that generated the event
  string meta_client_geo_country = 43;
  // Country code of the client that generated the event
  string meta_client_geo_country_code = 44;
  // Continent code of the client that generated the event
  string meta_client_geo_continent_code = 45;
  // Longitude of the client that generated the event
  google.protobuf.DoubleValue meta_client_geo_longitude = 46;
  // Latitude of the client that generated the event
  google.protobuf.DoubleValue meta_client_geo_latitude = 47;
  // Autonomous system number of the client that generated the event
  google.protobuf.UInt32Value meta_client_geo_autonomous_system_number = 48;
  // Autonomous system organization of the client that generated the event
  google.protobuf.StringValue meta_client_geo_autonomous_system_organization = 49;
  // Name of the network associated with the client
  string meta_network_name = 50;
}

// Request for listing libp2p_gossipsub_aggregate_and_proof records
message ListLibp2pGossipsubAggregateAndProofRequest {
  // Filter by meta_network_name - Name of the network associated with the client (PRIMARY KEY - required)
  StringFilter meta_network_name = 1;

  // Filter by slot_start_date_time - Start date and time of the slot (ORDER BY column 2 - optional)
  UInt32Filter slot_start_date_time = 2;

  // Filter by meta_client_name - Name of the client that generated the event (ORDER BY column 3 - optional)
  StringFilter meta_client_name = 3;

  // Filter by peer_id_unique_key - Unique key associated with the identifier of the peer (ORDER BY column 4 - optional)
  Int64Filter peer_id_unique_key = 4;

  // Filter by message_id - Identifier of the message (ORDER BY column 5 - optional)
  StringFilter message_id = 5;

  // Filter by updated_date_time - Timestamp when the record was last updated (optional)
  UInt32Filter updated_date_time = 6;
  // Filter by event_date_time - Timestamp of the event with millisecond precision (optional)
  Int64Filter event_date_time = 7;
  // Filter by slot - Slot number associated with the event (optional)
  UInt32Filter slot = 8;
  // Filter by epoch - Epoch number associated with the event (optional)
  UInt32Filter epoch = 9;
  // Filter by epoch_start_date_time - Start date and time of the epoch (optional)
  UInt32Filter epoch_start_date_time = 10;
  // Filter by wallclock_slot - Slot number of the wall clock when the event was received (optional)
  UInt32Filter wallclock_slot = 11;
  // Filter by wallclock_slot_start_date_time - Start date and time of the wall clock slot when the event was received (optional)
  UInt32Filter wallclock_slot_start_date_time = 12;
  // Filter by wallclock_epoch - Epoch number of the wall clock when the event was received (optional)
  UInt32Filter wallclock_epoch = 13;
  // Filter by wallclock_epoch_start_date_time - Start date and time of the wall clock epoch when the event was received (optional)
  UInt32Filter wallclock_epoch_start_date_time = 14;
  // Filter by propagation_slot_start_diff - Difference in slot start time for propagation (optional)
  UInt32Filter propagation_slot_start_diff = 15;
  // Filter by message_size - Size of the message in bytes (optional)
  UInt32Filter message_size = 16;
  // Filter by topic_layer - Layer of the topic in the gossipsub protocol (optional)
  StringFilter topic_layer = 17;
  // Filter by topic_fork_digest_value - Fork digest value of the topic (optional)
  StringFilter topic_fork_digest_value = 18;
  // Filter by topic_name - Name of the topic (optional)
  StringFilter topic_name = 19;
  // Filter by topic_encoding - Encoding used for the topic (optional)
  StringFilter topic_encoding = 20;
  // Filter by aggregator_index - Index of the validator who created this aggregate (optional)
  UInt32Filter aggregator_index = 21;
  // Filter by committee_index - Committee index from the attestation (optional)
  StringFilter committee_index = 22;
  // Filter by aggregation_bits - Bitfield of aggregated attestation (optional)
  StringFilter aggregation_bits = 23;
  // Filter by beacon_block_root - Root of the beacon block being attested to (optional)
  StringFilter beacon_block_root = 24;
  // Filter by source_epoch - Source epoch from the attestation (optional)
  UInt32Filter source_epoch = 25;
  // Filter by source_root - Source root from the attestation (optional)
  StringFilter source_root = 26;
  // Filter by target_epoch - Target epoch from the attestation (optional)
  UInt32Filter target_epoch = 27;
  // Filter by target_root - Target root from the attestation (optional)
  StringFilter target_root = 28;
  // Filter by meta_client_version - Version of the client that generated the event (optional)
  StringFilter meta_client_version = 29;
  // Filter by meta_client_implementation - Implementation of the client that generated the event (optional)
  StringFilter meta_client_implementation = 30;
  // Filter by meta_client_os - Operating system of the client that generated the event (optional)
  StringFilter meta_client_os = 31;
  // Filter by meta_client_ip - IP address of the client that generated the event (optional)
  NullableStringFilter meta_client_ip = 32;
  // Filter by meta_client_geo_city - City of the client that generated the event (optional)
  StringFilter meta_client_geo_city = 33;
  // Filter by meta_client_geo_country - Country of the client that generated the event (optional)
  StringFilter meta_client_geo_country = 34;
  // Filter by meta_client_geo_country_code - Country code of the client that generated the event (optional)
  StringFilter meta_client_geo_country_code = 35;
  // Filter by meta_client_geo_continent_code - Continent code of the client that generated the event (optional)
  StringFilter meta_client_geo_continent_code = 36;
  // Filter by meta_client_geo_longitude - Longitude of the client that generated the event (optional)
  google.protobuf.DoubleValue meta_client_geo_longitude = 37;
  // Filter by meta_client_geo_latitude - Latitude of the client that generated the event (optional)
  google.protobuf.DoubleValue meta_client_geo_latitude = 38;
  // Filter by meta_client_geo_autonomous_system_number - Autonomous system number of the client that generated the event (optional)
  NullableUInt32Filter meta_client_geo_autonomous_system_number = 39;
  // Filter by meta_client_geo_autonomous_system_organization - Autonomous system organization of the client that generated the event (optional)
  NullableStringFilter meta_client_geo_autonomous_system_organization = 40;

  // The maximum number of libp2p_gossipsub_aggregate_and_proof to return.
  // If unspecified, at most 100 items will be returned.
  // The maximum value is 10000; values above 10000 will be coerced to 10000.
  int32 page_size = 41;
  // A page token, received from a previous `ListLibp2pGossipsubAggregateAndProof` call.
  // Provide this to retrieve the subsequent page.
  string page_token = 42;
  // The order of results. Format: comma-separated list of fields.
  // Example: "foo,bar" or "foo desc,bar" for descending order on foo.
  // If unspecified, results will be returned in the default order.
  string order_by = 43;
}

// Response for listing libp2p_gossipsub_aggregate_and_proof records
message ListLibp2pGossipsubAggregateAndProofResponse {
  // The list of libp2p_gossipsub_aggregate_and_proof.
  repeated Libp2pGossipsubAggregateAndProof libp2p_gossipsub_aggregate_and_proof = 1;
  // A token, which can be sent as `page_token` to retrieve the next page.
  // If this field is omitted, there are no subsequent pages.
  string next_page_token = 2;
}

// Request for getting a single libp2p_gossipsub_aggregate_and_proof record by primary key
message GetLibp2pGossipsubAggregateAndProofRequest {
  // Name of the network associated with the client
  string meta_network_name = 1; // Primary key (required)
}

// Response for getting a single libp2p_gossipsub_aggregate_and_proof record
message GetLibp2pGossipsubAggregateAndProofResponse {
  Libp2pGossipsubAggregateAndProof item = 1;
}

// Query libp2p_gossipsub_aggregate_and_proof data
service Libp2pGossipsubAggregateAndProofService {
  // List records | Retrieve paginated results with optional filtering
  rpc List(ListLibp2pGossipsubAggregateAndProofRequest) returns (ListLibp2pGossipsubAggregateAndProofResponse);
  // Get record | Retrieve a single record by primary key
  rpc Get(GetLibp2pGossipsubAggregateAndProofRequest) returns (GetLibp2pGossipsubAggregateAndProofResponse);
}
