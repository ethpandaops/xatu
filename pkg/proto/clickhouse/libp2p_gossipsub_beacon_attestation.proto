syntax = "proto3";

package clickhouse.v1;

import "common.proto";
import "google/protobuf/wrappers.proto";

option go_package = "github.com/ethpandaops/xatu/pkg/proto/clickhouse";
// Table for libp2p gossipsub beacon attestation data.

message Libp2pGossipsubBeaconAttestation {
  // Timestamp when the record was last updated
  uint32 updated_date_time = 11;
  // Timestamp of the event with millisecond precision
  int64 event_date_time = 12;
  // Slot number associated with the event
  uint32 slot = 13;
  // Start date and time of the slot
  uint32 slot_start_date_time = 14;
  // The epoch number in the attestation
  uint32 epoch = 15;
  // The wall clock time when the epoch started
  uint32 epoch_start_date_time = 16;
  // The committee index in the attestation
  string committee_index = 17;
  // The index of the validator attesting to the event
  google.protobuf.UInt32Value attesting_validator_index = 18;
  // The committee index of the attesting validator
  string attesting_validator_committee_index = 19;
  // Slot number of the wall clock when the event was received
  uint32 wallclock_slot = 20;
  // Start date and time of the wall clock slot when the event was received
  uint32 wallclock_slot_start_date_time = 21;
  // Epoch number of the wall clock when the event was received
  uint32 wallclock_epoch = 22;
  // Start date and time of the wall clock epoch when the event was received
  uint32 wallclock_epoch_start_date_time = 23;
  // Difference in slot start time for propagation
  uint32 propagation_slot_start_diff = 24;
  // Unique key associated with the identifier of the peer
  int64 peer_id_unique_key = 25;
  // Identifier of the message
  string message_id = 26;
  // Size of the message in bytes
  uint32 message_size = 27;
  // Layer of the topic in the gossipsub protocol
  string topic_layer = 28;
  // Fork digest value of the topic
  string topic_fork_digest_value = 29;
  // Name of the topic
  string topic_name = 30;
  // Encoding used for the topic
  string topic_encoding = 31;
  // The aggregation bits of the event in the attestation
  string aggregation_bits = 32;
  // The beacon block root hash in the attestation
  string beacon_block_root = 33;
  // The source epoch number in the attestation
  uint32 source_epoch = 34;
  // The wall clock time when the source epoch started
  uint32 source_epoch_start_date_time = 35;
  // The source beacon block root hash in the attestation
  string source_root = 36;
  // The target epoch number in the attestation
  uint32 target_epoch = 37;
  // The wall clock time when the target epoch started
  uint32 target_epoch_start_date_time = 38;
  // The target beacon block root hash in the attestation
  string target_root = 39;
  // Name of the client that generated the event
  string meta_client_name = 40;
  // Version of the client that generated the event
  string meta_client_version = 41;
  // Implementation of the client that generated the event
  string meta_client_implementation = 42;
  // Operating system of the client that generated the event
  string meta_client_os = 43;
  // IP address of the client that generated the event
  google.protobuf.StringValue meta_client_ip = 44;
  // City of the client that generated the event
  string meta_client_geo_city = 45;
  // Country of the client that generated the event
  string meta_client_geo_country = 46;
  // Country code of the client that generated the event
  string meta_client_geo_country_code = 47;
  // Continent code of the client that generated the event
  string meta_client_geo_continent_code = 48;
  // Longitude of the client that generated the event
  google.protobuf.DoubleValue meta_client_geo_longitude = 49;
  // Latitude of the client that generated the event
  google.protobuf.DoubleValue meta_client_geo_latitude = 50;
  // Autonomous system number of the client that generated the event
  google.protobuf.UInt32Value meta_client_geo_autonomous_system_number = 51;
  // Autonomous system organization of the client that generated the event
  google.protobuf.StringValue meta_client_geo_autonomous_system_organization = 52;
  // Name of the network associated with the client
  string meta_network_name = 53;
}

// Request for listing libp2p_gossipsub_beacon_attestation records
message ListLibp2pGossipsubBeaconAttestationRequest {
  // Filter by meta_network_name - Name of the network associated with the client (PRIMARY KEY - required)
  StringFilter meta_network_name = 1;

  // Filter by slot_start_date_time - Start date and time of the slot (ORDER BY column 2 - optional)
  UInt32Filter slot_start_date_time = 2;

  // Filter by meta_client_name - Name of the client that generated the event (ORDER BY column 3 - optional)
  StringFilter meta_client_name = 3;

  // Filter by peer_id_unique_key - Unique key associated with the identifier of the peer (ORDER BY column 4 - optional)
  Int64Filter peer_id_unique_key = 4;

  // Filter by message_id - Identifier of the message (ORDER BY column 5 - optional)
  StringFilter message_id = 5;

  // Filter by updated_date_time - Timestamp when the record was last updated (optional)
  UInt32Filter updated_date_time = 6;
  // Filter by event_date_time - Timestamp of the event with millisecond precision (optional)
  Int64Filter event_date_time = 7;
  // Filter by slot - Slot number associated with the event (optional)
  UInt32Filter slot = 8;
  // Filter by epoch - The epoch number in the attestation (optional)
  UInt32Filter epoch = 9;
  // Filter by epoch_start_date_time - The wall clock time when the epoch started (optional)
  UInt32Filter epoch_start_date_time = 10;
  // Filter by committee_index - The committee index in the attestation (optional)
  StringFilter committee_index = 11;
  // Filter by attesting_validator_index - The index of the validator attesting to the event (optional)
  NullableUInt32Filter attesting_validator_index = 12;
  // Filter by attesting_validator_committee_index - The committee index of the attesting validator (optional)
  StringFilter attesting_validator_committee_index = 13;
  // Filter by wallclock_slot - Slot number of the wall clock when the event was received (optional)
  UInt32Filter wallclock_slot = 14;
  // Filter by wallclock_slot_start_date_time - Start date and time of the wall clock slot when the event was received (optional)
  UInt32Filter wallclock_slot_start_date_time = 15;
  // Filter by wallclock_epoch - Epoch number of the wall clock when the event was received (optional)
  UInt32Filter wallclock_epoch = 16;
  // Filter by wallclock_epoch_start_date_time - Start date and time of the wall clock epoch when the event was received (optional)
  UInt32Filter wallclock_epoch_start_date_time = 17;
  // Filter by propagation_slot_start_diff - Difference in slot start time for propagation (optional)
  UInt32Filter propagation_slot_start_diff = 18;
  // Filter by message_size - Size of the message in bytes (optional)
  UInt32Filter message_size = 19;
  // Filter by topic_layer - Layer of the topic in the gossipsub protocol (optional)
  StringFilter topic_layer = 20;
  // Filter by topic_fork_digest_value - Fork digest value of the topic (optional)
  StringFilter topic_fork_digest_value = 21;
  // Filter by topic_name - Name of the topic (optional)
  StringFilter topic_name = 22;
  // Filter by topic_encoding - Encoding used for the topic (optional)
  StringFilter topic_encoding = 23;
  // Filter by aggregation_bits - The aggregation bits of the event in the attestation (optional)
  StringFilter aggregation_bits = 24;
  // Filter by beacon_block_root - The beacon block root hash in the attestation (optional)
  StringFilter beacon_block_root = 25;
  // Filter by source_epoch - The source epoch number in the attestation (optional)
  UInt32Filter source_epoch = 26;
  // Filter by source_epoch_start_date_time - The wall clock time when the source epoch started (optional)
  UInt32Filter source_epoch_start_date_time = 27;
  // Filter by source_root - The source beacon block root hash in the attestation (optional)
  StringFilter source_root = 28;
  // Filter by target_epoch - The target epoch number in the attestation (optional)
  UInt32Filter target_epoch = 29;
  // Filter by target_epoch_start_date_time - The wall clock time when the target epoch started (optional)
  UInt32Filter target_epoch_start_date_time = 30;
  // Filter by target_root - The target beacon block root hash in the attestation (optional)
  StringFilter target_root = 31;
  // Filter by meta_client_version - Version of the client that generated the event (optional)
  StringFilter meta_client_version = 32;
  // Filter by meta_client_implementation - Implementation of the client that generated the event (optional)
  StringFilter meta_client_implementation = 33;
  // Filter by meta_client_os - Operating system of the client that generated the event (optional)
  StringFilter meta_client_os = 34;
  // Filter by meta_client_ip - IP address of the client that generated the event (optional)
  NullableStringFilter meta_client_ip = 35;
  // Filter by meta_client_geo_city - City of the client that generated the event (optional)
  StringFilter meta_client_geo_city = 36;
  // Filter by meta_client_geo_country - Country of the client that generated the event (optional)
  StringFilter meta_client_geo_country = 37;
  // Filter by meta_client_geo_country_code - Country code of the client that generated the event (optional)
  StringFilter meta_client_geo_country_code = 38;
  // Filter by meta_client_geo_continent_code - Continent code of the client that generated the event (optional)
  StringFilter meta_client_geo_continent_code = 39;
  // Filter by meta_client_geo_longitude - Longitude of the client that generated the event (optional)
  google.protobuf.DoubleValue meta_client_geo_longitude = 40;
  // Filter by meta_client_geo_latitude - Latitude of the client that generated the event (optional)
  google.protobuf.DoubleValue meta_client_geo_latitude = 41;
  // Filter by meta_client_geo_autonomous_system_number - Autonomous system number of the client that generated the event (optional)
  NullableUInt32Filter meta_client_geo_autonomous_system_number = 42;
  // Filter by meta_client_geo_autonomous_system_organization - Autonomous system organization of the client that generated the event (optional)
  NullableStringFilter meta_client_geo_autonomous_system_organization = 43;

  // The maximum number of libp2p_gossipsub_beacon_attestation to return.
  // If unspecified, at most 100 items will be returned.
  // The maximum value is 10000; values above 10000 will be coerced to 10000.
  int32 page_size = 44;
  // A page token, received from a previous `ListLibp2pGossipsubBeaconAttestation` call.
  // Provide this to retrieve the subsequent page.
  string page_token = 45;
  // The order of results. Format: comma-separated list of fields.
  // Example: "foo,bar" or "foo desc,bar" for descending order on foo.
  // If unspecified, results will be returned in the default order.
  string order_by = 46;
}

// Response for listing libp2p_gossipsub_beacon_attestation records
message ListLibp2pGossipsubBeaconAttestationResponse {
  // The list of libp2p_gossipsub_beacon_attestation.
  repeated Libp2pGossipsubBeaconAttestation libp2p_gossipsub_beacon_attestation = 1;
  // A token, which can be sent as `page_token` to retrieve the next page.
  // If this field is omitted, there are no subsequent pages.
  string next_page_token = 2;
}

// Request for getting a single libp2p_gossipsub_beacon_attestation record by primary key
message GetLibp2pGossipsubBeaconAttestationRequest {
  // Name of the network associated with the client
  string meta_network_name = 1; // Primary key (required)
}

// Response for getting a single libp2p_gossipsub_beacon_attestation record
message GetLibp2pGossipsubBeaconAttestationResponse {
  Libp2pGossipsubBeaconAttestation item = 1;
}

// Query libp2p_gossipsub_beacon_attestation data
service Libp2pGossipsubBeaconAttestationService {
  // List records | Retrieve paginated results with optional filtering
  rpc List(ListLibp2pGossipsubBeaconAttestationRequest) returns (ListLibp2pGossipsubBeaconAttestationResponse);
  // Get record | Retrieve a single record by primary key
  rpc Get(GetLibp2pGossipsubBeaconAttestationRequest) returns (GetLibp2pGossipsubBeaconAttestationResponse);
}
