syntax = "proto3";

package clickhouse.v1;

import "common.proto";
import "google/protobuf/wrappers.proto";

option go_package = "github.com/ethpandaops/xatu/pkg/proto/clickhouse";
// Contains the details of the "I have" control messages from the peer.

message Libp2pRpcMetaControlIhave {
  // Unique identifier for each "I have" control record
  int64 unique_key = 11;
  // Timestamp when the "I have" control record was last updated
  uint32 updated_date_time = 12;
  // Timestamp of the "I have" control event
  int64 event_date_time = 13;
  // Unique key associated with the "I have" control metadata
  int64 rpc_meta_unique_key = 14;
  // Position in the RPC meta control IWANT message_ids array
  int32 message_index = 15;
  // Position in the RPC meta control IWANT array
  int32 control_index = 16;
  // Layer of the topic
  string topic_layer = 17;
  // Fork digest value of the topic
  string topic_fork_digest_value = 18;
  // Name of the topic
  string topic_name = 19;
  // Encoding of the topic
  string topic_encoding = 20;
  // Identifier of the message associated with the "I have" control
  string message_id = 21;
  // Unique key associated with the identifier of the peer involved in the I have control
  int64 peer_id_unique_key = 22;
  // Name of the client that generated the event
  string meta_client_name = 23;
  // Version of the client that generated the event
  string meta_client_version = 24;
  // Implementation of the client that generated the event
  string meta_client_implementation = 25;
  // Operating system of the client that generated the event
  string meta_client_os = 26;
  // IP address of the client that generated the event
  google.protobuf.StringValue meta_client_ip = 27;
  // City of the client that generated the event
  string meta_client_geo_city = 28;
  // Country of the client that generated the event
  string meta_client_geo_country = 29;
  // Country code of the client that generated the event
  string meta_client_geo_country_code = 30;
  // Continent code of the client that generated the event
  string meta_client_geo_continent_code = 31;
  // Longitude of the client that generated the event
  google.protobuf.DoubleValue meta_client_geo_longitude = 32;
  // Latitude of the client that generated the event
  google.protobuf.DoubleValue meta_client_geo_latitude = 33;
  // Autonomous system number of the client that generated the event
  google.protobuf.UInt32Value meta_client_geo_autonomous_system_number = 34;
  // Autonomous system organization of the client that generated the event
  google.protobuf.StringValue meta_client_geo_autonomous_system_organization = 35;
  // Ethereum network name
  string meta_network_name = 36;
}

// Request for listing libp2p_rpc_meta_control_ihave records
message ListLibp2pRpcMetaControlIhaveRequest {
  // Filter by meta_network_name - Ethereum network name (PRIMARY KEY - required)
  StringFilter meta_network_name = 1;

  // Filter by event_date_time - Timestamp of the "I have" control event (ORDER BY column 2 - optional)
  Int64Filter event_date_time = 2;

  // Filter by unique_key - Unique identifier for each "I have" control record (ORDER BY column 3 - optional)
  Int64Filter unique_key = 3;

  // Filter by control_index - Position in the RPC meta control IWANT array (ORDER BY column 4 - optional)
  Int32Filter control_index = 4;

  // Filter by message_index - Position in the RPC meta control IWANT message_ids array (ORDER BY column 5 - optional)
  Int32Filter message_index = 5;

  // Filter by meta_client_name - Name of the client that generated the event (ORDER BY column 6 - optional)
  StringFilter meta_client_name = 6;

  // Filter by updated_date_time - Timestamp when the "I have" control record was last updated (optional)
  UInt32Filter updated_date_time = 7;
  // Filter by rpc_meta_unique_key - Unique key associated with the "I have" control metadata (optional)
  Int64Filter rpc_meta_unique_key = 8;
  // Filter by topic_layer - Layer of the topic (optional)
  StringFilter topic_layer = 9;
  // Filter by topic_fork_digest_value - Fork digest value of the topic (optional)
  StringFilter topic_fork_digest_value = 10;
  // Filter by topic_name - Name of the topic (optional)
  StringFilter topic_name = 11;
  // Filter by topic_encoding - Encoding of the topic (optional)
  StringFilter topic_encoding = 12;
  // Filter by message_id - Identifier of the message associated with the "I have" control (optional)
  StringFilter message_id = 13;
  // Filter by peer_id_unique_key - Unique key associated with the identifier of the peer involved in the I have control (optional)
  Int64Filter peer_id_unique_key = 14;
  // Filter by meta_client_version - Version of the client that generated the event (optional)
  StringFilter meta_client_version = 15;
  // Filter by meta_client_implementation - Implementation of the client that generated the event (optional)
  StringFilter meta_client_implementation = 16;
  // Filter by meta_client_os - Operating system of the client that generated the event (optional)
  StringFilter meta_client_os = 17;
  // Filter by meta_client_ip - IP address of the client that generated the event (optional)
  NullableStringFilter meta_client_ip = 18;
  // Filter by meta_client_geo_city - City of the client that generated the event (optional)
  StringFilter meta_client_geo_city = 19;
  // Filter by meta_client_geo_country - Country of the client that generated the event (optional)
  StringFilter meta_client_geo_country = 20;
  // Filter by meta_client_geo_country_code - Country code of the client that generated the event (optional)
  StringFilter meta_client_geo_country_code = 21;
  // Filter by meta_client_geo_continent_code - Continent code of the client that generated the event (optional)
  StringFilter meta_client_geo_continent_code = 22;
  // Filter by meta_client_geo_longitude - Longitude of the client that generated the event (optional)
  google.protobuf.DoubleValue meta_client_geo_longitude = 23;
  // Filter by meta_client_geo_latitude - Latitude of the client that generated the event (optional)
  google.protobuf.DoubleValue meta_client_geo_latitude = 24;
  // Filter by meta_client_geo_autonomous_system_number - Autonomous system number of the client that generated the event (optional)
  NullableUInt32Filter meta_client_geo_autonomous_system_number = 25;
  // Filter by meta_client_geo_autonomous_system_organization - Autonomous system organization of the client that generated the event (optional)
  NullableStringFilter meta_client_geo_autonomous_system_organization = 26;

  // The maximum number of libp2p_rpc_meta_control_ihave to return.
  // If unspecified, at most 100 items will be returned.
  // The maximum value is 10000; values above 10000 will be coerced to 10000.
  int32 page_size = 27;
  // A page token, received from a previous `ListLibp2pRpcMetaControlIhave` call.
  // Provide this to retrieve the subsequent page.
  string page_token = 28;
  // The order of results. Format: comma-separated list of fields.
  // Example: "foo,bar" or "foo desc,bar" for descending order on foo.
  // If unspecified, results will be returned in the default order.
  string order_by = 29;
}

// Response for listing libp2p_rpc_meta_control_ihave records
message ListLibp2pRpcMetaControlIhaveResponse {
  // The list of libp2p_rpc_meta_control_ihave.
  repeated Libp2pRpcMetaControlIhave libp2p_rpc_meta_control_ihave = 1;
  // A token, which can be sent as `page_token` to retrieve the next page.
  // If this field is omitted, there are no subsequent pages.
  string next_page_token = 2;
}

// Request for getting a single libp2p_rpc_meta_control_ihave record by primary key
message GetLibp2pRpcMetaControlIhaveRequest {
  // Ethereum network name
  string meta_network_name = 1; // Primary key (required)
}

// Response for getting a single libp2p_rpc_meta_control_ihave record
message GetLibp2pRpcMetaControlIhaveResponse {
  Libp2pRpcMetaControlIhave item = 1;
}

// Query libp2p_rpc_meta_control_ihave data
service Libp2pRpcMetaControlIhaveService {
  // List records | Retrieve paginated results with optional filtering
  rpc List(ListLibp2pRpcMetaControlIhaveRequest) returns (ListLibp2pRpcMetaControlIhaveResponse);
  // Get record | Retrieve a single record by primary key
  rpc Get(GetLibp2pRpcMetaControlIhaveRequest) returns (GetLibp2pRpcMetaControlIhaveResponse);
}
